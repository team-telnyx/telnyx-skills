# Migration Plan: Automated Survey Django

> Generated by the Telnyx Twilio Migration Skill
> Date: 2026-03-05
> Migration Agent: telnyx-twilio-migration

## Project Overview

| Field | Value |
|-------|-------|
| Project Root | `~/twilio-test-repos/automated-survey-django` |
| Primary Language(s) | Python |
| Framework(s) | Django 1.8.3 |
| Twilio Products in Use | Voice, Messaging |
| Estimated Files to Modify | 4 |
| Current Branch | `master` |

## Migration Scope

| Twilio Product | Telnyx Replacement | Files Affected | Complexity | Notes |
|---------------|--------------------|----------------|------------|-------|
| Voice | TeXML | 3 | Low | Uses VoiceResponse builder - convert to raw XML |
| Messaging | TeXML | 3 | Low | Uses MessagingResponse builder - convert to raw XML |

### Out of Scope

- Number porting — test will use existing Telnyx numbers
- Webhook signature validation - not in original code

## Decision Points

- [x] **Voice approach**: TeXML (XML-compatible) - Simple TwiML response builders, no advanced features
- [x] **Migration strategy**: Big-bang - Only 3 files to modify, all related

## Migration Order

- [x] **Phase 0: Prerequisites** — TELNYX_API_KEY validated, phone number +353857688030 collected, costs approved
- [x] **Phase 1: Discovery** — Twilio usage scanned, 4 files identified
- [x] **Phase 2: Planning** — Migration plan generated
- [ ] **Phase 3: Setup** — Migration branch, Telnyx SDK installed alongside Twilio, env vars updated
- [ ] **Phase 4: Voice + Messaging Migration** — Convert VoiceResponse/MessagingResponse to raw TeXML
- [ ] **Phase 5: Validation** — Full validation suite passed
- [ ] **Phase 6: Cleanup** — Twilio SDK removed, report generated

## Files to Migrate

1. `automated_survey/views/questions.py` — Uses VoiceResponse, MessagingResponse
2. `automated_survey/views/question_responses.py` — Uses VoiceResponse, MessagingResponse
3. `automated_survey/views/surveys.py` — Uses VoiceResponse, MessagingResponse
4. `automated_survey/urls.py` — Contains `redirects_twilio_request_to_proper_endpoint` (rename only)

## Environment Changes

| Variable | Current (Twilio) | New (Telnyx) | Notes |
|----------|-----------------|--------------|-------|
| N/A | N/A | N/A | No env vars currently used for Twilio (uses SDK only) |

## Key Changes Required

### Code Changes
1. Replace `from twilio.twiml.voice_response import VoiceResponse` with raw XML string building
2. Replace `from twilio.twiml.messaging_response import MessagingResponse` with raw XML string building
3. Replace `VoiceResponse()` with raw TeXML Response XML
4. Replace `MessagingResponse()` with raw TeXML Response XML
5. Replace `response.say()` with `<Say>` tags
6. Replace `response.message()` with `<Message>` or `<Sms>` tags
7. Replace `response.gather()` with `<Gather>` tags
8. Replace `response.record()` with `<Record>` tags
9. Replace `response.hangup()` with `<Hangup/>`
10. Replace `response.redirect()` with `<Redirect>` tags

### URL Rename
- Function `redirects_twilio_request_to_proper_endpoint` → `redirects_telnyx_request_to_proper_endpoint`

## Risks & Mitigations

| Risk | Likelihood | Impact | Mitigation |
|------|-----------|--------|------------|
| TeXML verb differences | Low | Low | Verify each verb against reference |
| Recording behavior (dual vs single channel) | Low | Medium | Default is dual, matches Telnyx |

## Rollback Plan

1. `git checkout master` to restore original code
2. Twilio SDK remains in requirements.txt until Phase 6
